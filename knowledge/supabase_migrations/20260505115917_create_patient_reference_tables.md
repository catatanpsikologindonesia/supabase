# Migration: create_patient_reference_tables

- **Timestamp**: 20260505115917
- **Applied At**: 2026-05-05 11:59:17

## Description
Auto-generated migration for database structural changes.

## SQL Content
```sql
DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace WHERE n.nspname = 'public' AND c.relname = 'religion') THEN CREATE TABLE public.religion ( id uuid DEFAULT gen_random_uuid() NOT NULL PRIMARY KEY, name text NOT NULL, order_index integer NOT NULL DEFAULT 0, created_at timestamptz DEFAULT now() NOT NULL, updated_at timestamptz DEFAULT now() NOT NULL, created_by uuid REFERENCES auth.users(id), updated_by uuid REFERENCES auth.users(id) ); END IF; IF NOT EXISTS (SELECT 1 FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace WHERE n.nspname = 'public' AND c.relname = 'education') THEN CREATE TABLE public.education ( id uuid DEFAULT gen_random_uuid() NOT NULL PRIMARY KEY, name text NOT NULL, order_index integer NOT NULL DEFAULT 0, created_at timestamptz DEFAULT now() NOT NULL, updated_at timestamptz DEFAULT now() NOT NULL, created_by uuid REFERENCES auth.users(id), updated_by uuid REFERENCES auth.users(id) ); END IF; IF NOT EXISTS (SELECT 1 FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace WHERE n.nspname = 'public' AND c.relname = 'occupation') THEN CREATE TABLE public.occupation ( id uuid DEFAULT gen_random_uuid() NOT NULL PRIMARY KEY, name text NOT NULL, order_index integer NOT NULL DEFAULT 0, created_at timestamptz DEFAULT now() NOT NULL, updated_at timestamptz DEFAULT now() NOT NULL, created_by uuid REFERENCES auth.users(id), updated_by uuid REFERENCES auth.users(id) ); END IF; END $$; ALTER TABLE public.religion ENABLE ROW LEVEL SECURITY; ALTER TABLE public.education ENABLE ROW LEVEL SECURITY; ALTER TABLE public.occupation ENABLE ROW LEVEL SECURITY; CREATE UNIQUE INDEX IF NOT EXISTS religion_name_unique_idx ON public.religion (lower(name)); CREATE UNIQUE INDEX IF NOT EXISTS education_name_unique_idx ON public.education (lower(name)); CREATE UNIQUE INDEX IF NOT EXISTS occupation_name_unique_idx ON public.occupation (lower(name)); DROP POLICY IF EXISTS religion_select_all ON public.religion; CREATE POLICY religion_select_all ON public.religion FOR SELECT TO anon, authenticated USING (true); DROP POLICY IF EXISTS religion_admin_insert ON public.religion; CREATE POLICY religion_admin_insert ON public.religion FOR INSERT TO authenticated WITH CHECK (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS religion_admin_update ON public.religion; CREATE POLICY religion_admin_update ON public.religion FOR UPDATE TO authenticated USING (public.is_admin_at_least('STAFF')) WITH CHECK (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS religion_admin_delete ON public.religion; CREATE POLICY religion_admin_delete ON public.religion FOR DELETE TO authenticated USING (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS education_select_all ON public.education; CREATE POLICY education_select_all ON public.education FOR SELECT TO anon, authenticated USING (true); DROP POLICY IF EXISTS education_admin_insert ON public.education; CREATE POLICY education_admin_insert ON public.education FOR INSERT TO authenticated WITH CHECK (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS education_admin_update ON public.education; CREATE POLICY education_admin_update ON public.education FOR UPDATE TO authenticated USING (public.is_admin_at_least('STAFF')) WITH CHECK (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS education_admin_delete ON public.education; CREATE POLICY education_admin_delete ON public.education FOR DELETE TO authenticated USING (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS occupation_select_all ON public.occupation; CREATE POLICY occupation_select_all ON public.occupation FOR SELECT TO anon, authenticated USING (true); DROP POLICY IF EXISTS occupation_admin_insert ON public.occupation; CREATE POLICY occupation_admin_insert ON public.occupation FOR INSERT TO authenticated WITH CHECK (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS occupation_admin_update ON public.occupation; CREATE POLICY occupation_admin_update ON public.occupation FOR UPDATE TO authenticated USING (public.is_admin_at_least('STAFF')) WITH CHECK (public.is_admin_at_least('STAFF')); DROP POLICY IF EXISTS occupation_admin_delete ON public.occupation; CREATE POLICY occupation_admin_delete ON public.occupation FOR DELETE TO authenticated USING (public.is_admin_at_least('STAFF')); GRANT SELECT ON public.religion, public.education, public.occupation TO anon, authenticated, service_role; GRANT INSERT, UPDATE, DELETE ON public.religion, public.education, public.occupation TO authenticated; INSERT INTO public.religion (name, order_index) VALUES ('Islam', 1), ('Kristen Protestan', 2), ('Katolik', 3), ('Hindu', 4), ('Buddha', 5), ('Konghucu', 6), ('Lainnya', 7) ON CONFLICT DO NOTHING; INSERT INTO public.education (name, order_index) VALUES ('Belum Sekolah', 1), ('TK A', 2), ('TK B', 3), ('SD', 4), ('SMP', 5), ('SMA/SMK', 6), ('D1', 7), ('D2', 8), ('D3', 9), ('S1', 10), ('S2', 11), ('S3', 12), ('Lainnya', 13) ON CONFLICT DO NOTHING; INSERT INTO public.occupation (name, order_index) VALUES ('Pelajar/Mahasiswa', 1), ('Karyawan Swasta', 2), ('Pegawai Negeri Sipil (PNS)', 3), ('Wiraswasta', 4), ('Ibu Rumah Tangga', 5), ('Tidak Bekerja', 6), ('Lainnya', 7) ON CONFLICT DO NOTHING;
```
